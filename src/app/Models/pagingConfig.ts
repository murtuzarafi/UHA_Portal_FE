export interface PagingConfig {
    currentPage: number;
    itemsPerPage: number;
    totalItems: number;
}

export interface PagingPndConfig {
    currentPndPage: number;
    itemsPndPerPage: number;
    totalPndItems: number;
}